# Daily-motivation-Dashboard
A simple React application that fetches motivational quotes from an API and displays them to the user. It uses React hooks (useState and useEffect) for state management and API calls, supports liking quotes, conditional rendering for loading state, and dynamic list rendering.
